# Copy into backend/app/.env or set on Render / Docker (same variable names as Flutter).
# Primary is tried first; secondary is used if the primary request fails.
OPENROUTER_API_KEY_PRIMARY=
OPENROUTER_API_KEY_SECONDARY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001
